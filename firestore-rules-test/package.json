{
  "name": "proof-firestore-rules-test",
  "private": true,
  "scripts": {
    "test": "mocha --timeout 15000 test/**/*.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.6.0",
    "mocha": "^11.1.0"
  }
}
